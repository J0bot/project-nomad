[package]
name = "xerboxion-plc"
description = "PL0XI0N Link Contract (PLC) v1 — shared types + WASM ABI for ploxions"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
