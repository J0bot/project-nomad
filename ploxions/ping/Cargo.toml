[package]
name = "ploxion-ping"
description = "Example ploxion: provides 'ping'; emits ping on init + on each host 'tick'"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
ploxion-sdk = { path = "../sdk" }
