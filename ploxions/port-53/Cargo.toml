[package]
name = "ploxion-port-53"
description = "WASM ploxion: le port 53 (dns) en ploxion (port-bion). Residu PortDef + port_ploxion!. Reseau-en-ploxions."
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
ploxion-sdk = { path = "../sdk" }
