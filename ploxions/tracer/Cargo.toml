[package]
name = "ploxion-tracer"
description = "Example ploxion: subscribes to ping/pong/tick and logs every event (demonstrates bus tracing)"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
ploxion-sdk = { path = "../sdk" }
