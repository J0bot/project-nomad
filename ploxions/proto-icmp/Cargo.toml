[package]
name = "ploxion-proto-icmp"
description = "WASM ploxion: le protocole ICMP en ploxion (protocol-bion). Residu ProtocolDef + protocol_ploxion! du SDK. Vague 1 du reseau-en-ploxions."
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
ploxion-sdk = { path = "../sdk" }
