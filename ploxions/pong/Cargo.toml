[package]
name = "ploxion-pong"
description = "Example ploxion: requires 'ping'; on each ping increments a counter and emits 'pong'"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
ploxion-sdk = { path = "../sdk" }
